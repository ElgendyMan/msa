"""LangGraph agent nodes for the Zero-Budget Autonomous Web Pentesting
Framework. Each module here implements exactly one node from the 16-node
architecture and exports a single ``def node_name(state: AppState) -> dict``
function that LangGraph can register."""
